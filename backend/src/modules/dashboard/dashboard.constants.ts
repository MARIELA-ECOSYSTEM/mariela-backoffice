/** Mesmo valor de `MESES_DISPONIVEIS` no mock (`src/services/mock/dashboard.mock.ts`). */
export const MESES_DISPONIVEIS = 6;

/** Quantas vendas recentes aparecem em "Últimas vendas" — mesmo valor do mock. */
export const QUANTIDADE_ULTIMAS_VENDAS = 6;

/** Quantos registros recentes aparecem nas listas de Clientes/Fornecedores — mesmo valor do mock. */
export const QUANTIDADE_RECENTES = 5;
